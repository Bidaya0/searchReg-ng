# 时间策略迭代搜索使用说明（集成反思机制）

## 概述

本系统现在支持三种时间控制策略，能够在指定时间内持续进行迭代搜索，并集成了**反思机制**，实现"在时间满足前不断打磨子问题"的功能。

## 核心特性

### 🔄 反思机制
- **自我诊断**：每轮迭代后分析结果质量和问题效果
- **问题优化**：基于反思结果重新设计、深化或微调问题
- **知识积累**：从每次搜索中学习并应用经验
- **智能调整**：根据质量趋势和缺失角度调整搜索策略

### ⏰ 时间策略
- **硬截止时间策略 (Hard)**：严格按照截止时间执行
- **软截止时间策略 (Soft)**：预留缓冲时间，逐步减少迭代强度
- **自适应策略 (Adaptive)**：根据进度和质量动态调整时间分配

## 时间策略类型

### 1. 硬截止时间策略 (Hard)
- **特点**：严格按照设定的截止时间执行
- **行为**：到达截止时间立即停止，不进行任何额外迭代
- **适用场景**：有严格时间限制的任务，如会议前准备

### 2. 软截止时间策略 (Soft)
- **特点**：在截止时间前预留缓冲时间
- **行为**：在截止时间前30分钟开始逐步减少迭代强度
- **适用场景**：需要预留时间进行最终整理的任务

### 3. 自适应策略 (Adaptive)
- **特点**：根据任务进度和质量动态调整时间分配
- **行为**：智能分配时间，优先保证质量，在时间允许时进行额外迭代
- **适用场景**：追求最佳质量的研究任务

## 使用方法

### 命令行使用

#### 基本用法
```bash
# 使用默认2小时时间迭代搜索
python main.py --time-iterations --topic "人工智能在医疗领域的应用"

# 指定持续时间
python main.py --time-iterations --duration 4 --topic "量子计算研究"

# 指定时间策略
python main.py --time-iterations --duration 2 --time-strategy adaptive --topic "区块链技术"
```

#### 使用运行脚本
```bash
# 2小时自适应迭代搜索
./run_long_running.sh "人工智能在医疗领域的应用" --duration 2 --time-iterations --strategy adaptive

# 硬截止时间策略
./run_long_running.sh "量子计算研究" --duration 3 --time-iterations --strategy hard

# 软截止时间策略
./run_long_running.sh "机器学习算法" --duration 6 --time-iterations --strategy soft
```

### 环境变量配置

在 `.env` 文件中设置以下参数：

```env
# 时间策略配置
HARD_STRATEGY_BUFFER_SECONDS=300
SOFT_STRATEGY_BUFFER_SECONDS=1800
SOFT_STRATEGY_REDUCTION_FACTOR=2
ADAPTIVE_HIGH_QUALITY_THRESHOLD=0.8
ADAPTIVE_LOW_QUALITY_THRESHOLD=0.5
ADAPTIVE_HIGH_QUALITY_FACTOR=3

# 迭代搜索配置
ITERATION_DEFAULT_DURATION_HOURS=2
ITERATION_MAX_QUESTIONS=25
ITERATION_DEEPENING_QUESTIONS=5
```

## 反思机制工作流程

### 1. 反思分析阶段
每轮迭代完成后，系统会：
- **质量评估**：分析当前迭代的整体质量分数和趋势
- **问题效果分析**：评估每个问题的搜索效果
- **缺失角度识别**：发现研究中的盲点和缺失角度
- **改进建议生成**：基于分析结果生成具体的改进方向

### 2. 问题优化阶段
基于反思分析结果，系统会：
- **问题重新设计**：对低质量问题进行完全重新设计
- **问题深化**：对中等质量问题增加深度和具体性
- **问题微调**：对高质量问题进行精准优化
- **角度补充**：生成缺失研究角度的新问题

### 3. 智能调整阶段
系统会根据反思结果：
- **调整迭代频率**：高质量时减少频率，低质量时增加频率
- **优化搜索策略**：根据缺失角度调整搜索重点
- **学习经验积累**：记录成功的问题模式和搜索策略

## 输出结果

### 控制台输出
```
=== 时间迭代搜索结果 ===
主题：人工智能在医疗领域的应用
总迭代次数：8
总运行时间：2.1小时
总搜索结果数：156
最终质量分数：0.847
时间策略：adaptive

=== 质量分数历史 ===
第1轮迭代：质量分数 0.623
第2轮迭代：质量分数 0.689
第3轮迭代：质量分数 0.734
第4轮迭代：质量分数 0.781
第5轮迭代：质量分数 0.812
第6轮迭代：质量分数 0.834
第7轮迭代：质量分数 0.841
第8轮迭代：质量分数 0.847

=== 执行统计 ===
平均每轮迭代时间：15.8分钟
迭代效率：3.8轮/小时

=== 反思机制统计 ===
反思分析次数：7
平均质量分数：0.765
质量趋势：improving
发现缺失角度总数：12
问题优化次数：7
平均优化效果：0.723
优化问题总数：89
优化类型分布：
  redesigned: 15个
  deepened: 35个
  refined: 25个
  generated: 14个
```

### 文件输出
- 进度报告：`./data/progress/progress_YYYYMMDD_HHMMSS.json`
- 检查点文件：`./data/checkpoints/checkpoint_YYYYMMDD_HHMMSS.json`
- 日志文件：`./data/logs/workflow_YYYYMMDD_HHMMSS.log`

## 测试验证

运行测试脚本验证反思机制功能：
```bash
# 测试反思机制
python test_reflection_mechanism.py

# 测试时间策略
python test_time_strategy.py
```

测试内容包括：
- 反思分析器的质量评估和缺失角度识别
- 问题优化器的重新设计、深化和微调功能
- 反思机制与时间策略的集成
- 端到端的反思工作流测试

## 性能优化建议

### 1. 资源管理
- 定期清理内存和临时文件
- 监控CPU和内存使用情况
- 设置合理的并发搜索数量

### 2. 时间分配
- 根据任务复杂度调整时间策略
- 使用自适应策略获得最佳效果
- 预留足够的缓冲时间

### 3. 质量提升
- 关注质量分数变化趋势
- 及时调整搜索策略
- 利用历史结果优化问题生成

## 故障排除

### 常见问题

1. **迭代提前结束**
   - 检查时间策略设置
   - 确认质量收敛阈值
   - 查看资源使用情况

2. **搜索超时**
   - 调整搜索超时设置
   - 减少并发搜索数量
   - 优化搜索策略

3. **内存不足**
   - 启用内存清理机制
   - 减少问题数量
   - 调整缓存设置

### 日志分析
查看日志文件了解详细执行情况：
```bash
tail -f ./data/logs/workflow_*.log
```

## 总结

时间策略迭代搜索功能现在已经完全实现，并集成了强大的反思机制，支持：
- ✅ 三种时间控制策略（硬/软/自适应）
- ✅ 2小时持续迭代搜索
- ✅ **反思机制**：每轮迭代后自我诊断和问题优化
- ✅ **问题打磨**：重新设计、深化、微调问题
- ✅ **智能调整**：基于质量趋势和缺失角度调整策略
- ✅ **知识积累**：从每次搜索中学习并应用经验
- ✅ 完整的错误处理和恢复机制

用户可以根据具体需求选择合适的时间策略，系统会自动在指定时间内持续进行迭代搜索，并通过反思机制不断打磨和优化子问题，最大化搜索质量和结果完整性。
